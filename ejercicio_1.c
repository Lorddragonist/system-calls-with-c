#include <stdio.h>
#include <stdlib.h>

int main()
{
    // Imprime el mensaje "Hola Mundo!" en la consola.
    printf("Hola Mundo!\n");

    // La función getchar() espera a que el usuario presione una tecla.
    getchar();

    // Retorna 0 para indicar que el programa se ejecutó con éxito.
    return 0;
}
